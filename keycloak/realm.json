{
    "realm": "wallet-realm",
    "enabled": true,
    "clients": [
        {
            "clientId": "wallet-app",
            "enabled": true,
            "protocol": "openid-connect",
            "publicClient": true,
            "directAccessGrantsEnabled": true
        }
    ]
}