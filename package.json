{
  "name": "ebsi-vector",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "husky install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "rds:up": "docker compose -f docker-compose-rds.yml up --remove-orphans --build",
    "rds:down": "docker compose -f docker-compose-rds.yml down --remove-orphans",
    "migration:create": "cd apps/backend && pnpm migration:create"
  },
  "keywords": [],
  "author": "",
  "license": "private",
  "devDependencies": {
    "husky": "^9.0.7"
  }
}