{
  "name": "krecential-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:up": "docker compose -f docker-compose.yml up --remove-orphans --build",
    "docker:down": "docker compose -f docker-compose.yml down --remove-orphans"
  },
  "keywords": [],
  "author": "",
  "license": "private",
  "devDependencies": {
    "@commitlint/cli": "^18.6.0",
    "@commitlint/config-conventional": "^18.6.0",
    "lint-staged": "^15.2.1"
  }
}